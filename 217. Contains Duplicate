/**
 * @param {number[]} nums
 * @return {boolean}
 */
var containsDuplicate = function(nums) {
    if(nums.length <= 1)
        return false;
    if(nums.length != new Set(nums).size)
        return true;
    return false;
};
